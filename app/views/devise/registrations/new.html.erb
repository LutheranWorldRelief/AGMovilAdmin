<div class="box">
  <h2>Registro de usuario</h2>

  <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
    <%= f.error_notification %>
    <div class="form-group">
      <%= f.input :names,
                  required: true,
                  label: "Nombres",
                  input_html: { class: "form-control" }%>
    </div><div class="form-group">
      <%= f.input :lastnames,
                  required: true,
                  label: "Apellidos",
                  input_html: { class: "form-control" }%>
    </div><div class="form-group">
      <%= f.input :email,
                  required: true,
                  autofocus: true ,
                  label: "Correo electrónico",
                  input_html: { class: "form-control", autocomplete: "email" }%>
    </div>
    <div class="form-group">
      <%= f.input :password,
                  required: true,
                  label: "Contraseña",
                  hint: ("#{@minimum_password_length} caracteres tamaño minimo" if @minimum_password_length),
                  input_html: { class: "form-control", autocomplete: "new-password" } %>
    </div>
    <div class="form-group">
      <%= f.input :password_confirmation,
                  required: true,
                  label: "Confirmar contraseña",
                  input_html: { class: "form-control", autocomplete: "new-password" } %>
    </div>
    <div class="form-group">
      <%= f.button :submit, "Registrarme", class: "btn btn-raised btn-primary" %>
    </div>
  <% end %>

  <%= render "devise/shared/links" %>
</div>
