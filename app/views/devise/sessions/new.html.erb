<div class="box">
  <h2>Iniciar sesión</h2>
  <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="form-group">
      <%= f.input :email,
                required: false,
                autofocus: true,
                label: "Correo electrónico",
                input_html: { class: "form-control", autocomplete: "email" } %>
    </div>
    <div class="form-group">
      <%= f.input :password,
                  required: false,
                  label: "Contraseña",
                  input_html: { class: "form-control", autocomplete: "current-password" } %>
    </div>
    <div class="form-group">
      <div class="checkbox">
        <%= f.input :remember_me, label: "Recordame siempre", as: :boolean if devise_mapping.rememberable? %>
      </div>
    </div>
    <div class="form-group">
      <%= f.button :submit, "Iniciar sesión", class: "btn btn-raised btn-primary" %>
    </div>
  <% end %>

  <%= render "devise/shared/links" %>
</div>
